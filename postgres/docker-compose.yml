services:
  postgres:
    image: postgres:16-alpine
    ports:
      - "5432:5432"
    environment:
      POSTGRES_USER: composcd
      POSTGRES_PASSWORD: secret
      POSTGRES_DB: testdb
    restart: unless-stopped
